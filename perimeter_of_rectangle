#include<stdio.h>
int main(){
    int l,b;
    printf("Enter l:");
    scanf("%d",&l);
    printf("Enter b:");
    scanf("%d",&b);
    printf("perimeter of the rectangle is:%d",2*(l+b));
    return 0;
    
}
